/// <reference types="node" />
import { Stats } from 'fs-extra';
/**
 * 接收一个数据队列和一个 generate 函数, 通过将数据传递给 generate 拿到一个 promise 对象
 * 根据 num 传入的并行个数进行适当任务的并发
 *
 * @param tasks[any[]]
 * @param generate[Function]
 * @param num[Number]
 */
export declare function parallel(tasks: any[], generate: Function, num?: number): Promise<any[]>;
declare type recursiveSyncFunction = (file: string, stat: Stats, name: string) => boolean;
declare type recursiveAsyncFunction = (file: string, stat: Stats, name: string) => Promise<boolean>;
/**
 * 同步版递归查询所有的文件夹
 * @param dir 搜索的文件夹的父文件夹路径
 * @param name 文件夹内的名字
 * @param fileHandle 文件处理函数
 * @param dirHandle 文件夹处理函数
 */
export declare function recursiveSync(dir: string, name: string, fileHandle: recursiveSyncFunction, dirHandle: recursiveSyncFunction): void;
/**
 * 异步版递归查询所有的文件夹，速度比同步慢，但是在扫描过程中，可以响应其他的 js 操作
 * @param dir 搜索的文件夹的父文件夹路径
 * @param name 文件夹内的名字
 * @param fileHandle 文件处理函数
 * @param dirHandle 文件夹处理函数
 */
export declare function recursiveAsync(dir: string, name: string, fileHandle: recursiveAsyncFunction, dirHandle: recursiveAsyncFunction): Promise<boolean | undefined>;
export {};
