import { WebContents } from 'electron';
import { RendererStatus } from '../common';
/** Creates a renderer ANR status hook */
export declare function createRendererAnrStatusHandler(): (status: RendererStatus, contents: WebContents) => void;
interface LegacyOptions {
    entryScript: string;
    pollInterval: number;
    anrThreshold: number;
    captureStackTrace: boolean;
    debug: boolean;
}
/**
 * @deprecated Use `Anr` integration instead.
 *
 * ```js
 * import { init, anrIntegration } from '@sentry/electron';
 *
 * init({
 *   dsn: "__DSN__",
 *   integrations: [anrIntegration({ captureStackTrace: true })],
 * });
 * ```
 */
export declare function enableMainProcessAnrDetection(options?: Partial<LegacyOptions>): Promise<void>;
export {};
//# sourceMappingURL=anr.d.ts.map