import { MeasurementUnit, MetricsAggregator, Primitive } from '@sentry/types';
/**
 * Sends metrics to the Electron main process where they can be aggregated in a single process
 */
export declare class ElectronRendererMetricsAggregator implements MetricsAggregator {
    private readonly _ipc;
    constructor();
    /** @inheritdoc */
    add(metricType: 'c' | 'g' | 's' | 'd', name: string, value: string | number, unit?: MeasurementUnit | undefined, tags?: Record<string, Primitive> | undefined, timestamp?: number | undefined): void;
    /** @inheritdoc */
    flush(): void;
    /** @inheritdoc */
    close(): void;
    /** @inheritdoc */
    toString(): string;
}
//# sourceMappingURL=metrics.d.ts.map