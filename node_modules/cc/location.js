const ps = require('path');

let location;

module.exports = {
    get: () => {
        return location;
    },

    set: (value) => {
        location = ps.join(value, 'bin', '.cache', 'dev', 'editor');
    },

    setDistribution(value) {
        location = value;
    },
};
