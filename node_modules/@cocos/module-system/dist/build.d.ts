interface BuildOptions {
    out: string;
    sourceMap: boolean;
    minify: boolean;
    platform: string;
    hmr?: boolean;
}
export declare function build({ out, sourceMap, minify, platform, hmr, }: BuildOptions): Promise<void>;
export {};
