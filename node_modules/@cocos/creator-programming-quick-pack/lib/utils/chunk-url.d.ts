/// <reference types="node" />
import { ChunkId } from './chunk-id';
import { URL } from 'url';
export declare function isQuickPackURL(url: URL): boolean;
export declare class FsLocator {
    constructor(pathname: string);
    getOrigin(): URL;
    toQuickPackURL(chunkId: string): URL;
    toChunkId(quickPackURL: URL): ChunkId;
    private _relateUrlEvaluator;
    private _fsRoot;
}
export declare const quickPackRootURL: Readonly<URL>;
