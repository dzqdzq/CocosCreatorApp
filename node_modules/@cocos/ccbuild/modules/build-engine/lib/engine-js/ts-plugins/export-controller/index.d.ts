import * as ts from '@cocos/typescript';
import { StatsQuery, ConfigInterface } from "../../../../../stats-query/lib/index";
export interface IExportControllerOptions {
  statsQuery: StatsQuery;
  context: ConfigInterface.Context;
}
export declare class ExportController {
  private readonly _context;
  private _currentProgram;
  private _currentSourceFile;
  private _typeChecker;
  private _options;
  constructor(context: ts.TransformationContext, options: IExportControllerOptions);
  visitSourceFile(node: ts.SourceFile, program: ts.Program, context: ts.TransformationContext): ts.SourceFile;
  private visitNodeAndChildren;
  private visitNode;
}
export declare function exportControllerTransformer(program: ts.Program, config: IExportControllerOptions): ts.TransformerFactory<ts.SourceFile>;