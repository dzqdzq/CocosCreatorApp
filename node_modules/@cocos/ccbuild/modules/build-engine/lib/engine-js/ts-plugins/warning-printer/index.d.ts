import * as ts from '@cocos/typescript';
export interface IWarningPrinterOptions {
  warnThisDotThreshold: number;
  warnNoConstructorFound: boolean;
}
export declare class WarningPrinter {
  private readonly _context;
  private readonly _options;
  private _currentProgram;
  private _currentSourceFile;
  private _typeChecker;
  constructor(context: ts.TransformationContext, options?: Partial<IWarningPrinterOptions>);
  visitSourceFile(node: ts.SourceFile, program: ts.Program, context: ts.TransformationContext): ts.SourceFile;
  private visitNodeAndChildren;
  private getNodeByKind;
  private visitNode;
  private checkHaveConstructor;
  private checkThisDotCountInBlock;
}
export declare function warningPrinterTransformer(program: ts.Program, config?: Partial<IWarningPrinterOptions>): ts.TransformerFactory<ts.SourceFile>;