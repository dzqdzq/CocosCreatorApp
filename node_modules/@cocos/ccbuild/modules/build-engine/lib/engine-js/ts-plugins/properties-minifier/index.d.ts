import * as ts from '@cocos/typescript';
export interface IMinifierOptions {
  /**
   * Prefix of generated names (e.g. '_ccprivate$')
   */
  prefix: string;
  mangleList: string[];
  dontMangleList: string[];
  mangleGetterSetter: boolean;
  mangleProtected: boolean;
  ignoreJsDocTag: boolean;
}
export declare class PropertiesMinifier {
  private readonly _context;
  private readonly _options;
  private _currentProgram;
  private _currentSourceFile;
  private _typeChecker;
  constructor(context: ts.TransformationContext, options?: Partial<IMinifierOptions>);
  visitSourceFile(node: ts.SourceFile, program: ts.Program, context: ts.TransformationContext): ts.SourceFile;
  private visitNodeAndChildren;
  private visitNode;
  private getSymbolForElementAccessExpressionWithQuestionDot;
  private createNewAccessExpression;
  private createNewBindingElement;
  private createNewNode;
  private getNewName;
  private isPrivateNonStatic;
  private hasJsDocTag;
  private isPrivate;
  private updatePrivateByOptions;
  private isParentSymbolPrivate;
  private hasModifier;
  private isAccessExpression;
  private isClassMember;
  private isInterfaceMember;
  private isConstructorParameter;
  private isConstructorParameterReference;
  private isPropertyInInterfaceNotShorthand;
  private getTypeSymbol;
  private isInterfacePropertyHasMangleTag;
  private isIdentifierInVariableDeclaration;
  private isIdentifierInArrayLiteralExpression;
  private isIdentifierInBinaryExpression;
  private isPropertyAssignment;
  private isPrivateNonStaticClassMember;
  private hasDecorators;
  private getModifiers;
  private isBreakingTypeScriptApi;
}
export declare function minifyPrivatePropertiesTransformer(program: ts.Program, config?: Partial<IMinifierOptions>): ts.TransformerFactory<ts.SourceFile>;