import { systemJSPrototype } from '../systemjs-source/src/system-core';

systemJSPrototype.instantiate = function (url, firstParentUrl) {
    throw new Error("You are now allowed to instantiate module " + url + " from " + firstParentUrl);
}