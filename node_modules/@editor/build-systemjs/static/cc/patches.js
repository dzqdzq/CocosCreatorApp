import { systemJSPrototype } from '../systemjs-source/src/system-core';
import { setImportMap } from './import-map';
import { hookInstantiationOverSchema } from './hook-schema';
import { setBaseUrl } from './set-base-url';

export const patches = {
    setBaseUrl,
    setImportMap,
    hookInstantiationOverSchema,
};

systemJSPrototype.patches = patches;