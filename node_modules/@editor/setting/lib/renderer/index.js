'use stirct';

const ipc = require('@base/electron-base-ipc');

const pkg = require('../../package.json');
const ipcFlag = `${pkg.name}@${pkg.version}`;

module.exports = ipc.sendSync(`${ipcFlag}:query`);
