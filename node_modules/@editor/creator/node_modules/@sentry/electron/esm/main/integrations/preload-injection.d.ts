/**
 * Injects the preload script into the provided sessions.
 *
 * Defaults to injecting into the defaultSession
 */
export declare const preloadInjectionIntegration: () => import("@sentry/types").IntegrationFnResult;
/**
 * Injects the preload script into the provided sessions.
 *
 * Defaults to injecting into the defaultSession
 *
 * @deprecated Use `preloadInjectionIntegration()` instead
 */
export declare const PreloadInjection: import("@sentry/types").IntegrationClass<import("@sentry/types").Integration>;
//# sourceMappingURL=preload-injection.d.ts.map