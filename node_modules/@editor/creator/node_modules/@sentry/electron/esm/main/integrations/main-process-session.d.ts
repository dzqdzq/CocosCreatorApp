export interface Options {
    /**
     * Whether sessions should be sent immediately on creation
     *
     * @default false
     */
    sendOnCreate?: boolean;
}
/** Tracks sessions as the main process lifetime. */
export declare const mainProcessSessionIntegration: (options?: Options | undefined) => import("@sentry/types").IntegrationFnResult;
/**
 * Tracks sessions as the main process lifetime.
 *
 * @deprecated Use `mainProcessSessionIntegration()` instead
 */
export declare const MainProcessSession: import("@sentry/types").IntegrationClass<import("@sentry/types").Integration>;
//# sourceMappingURL=main-process-session.d.ts.map