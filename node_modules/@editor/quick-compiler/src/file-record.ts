import type { FileUid } from './file-uid';

export interface FileRecord {
    failed?: boolean;
    fileTime: number;
    dependencies: FileUid[];
    externalDependencies: Record<string, string>;
    status: boolean;
}
