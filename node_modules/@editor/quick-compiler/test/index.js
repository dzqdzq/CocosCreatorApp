// @ts-check

console.time('summary');

const ps = require('path');
const { run } = require('./run');

(async () => {
    const engineRoot = ps.join('X:', 'Repos', 'Cocos', 'engine');
    await run(
        engineRoot,
        ps.join(engineRoot, 'bin', '.cache', 'dev'),
    );

    console.timeEnd('summary');
})();
