"use strict";const{join:join}=require("path"),{readdirSync:readdirSync,statSync:statSync}=require("fs-extra"),builder=require("@editor/build");function scan(e){readdirSync(e).forEach(i=>{const r=join(e,i);statSync(r).isDirectory()&&builder.addWorkspace(r)})}scan(join(__dirname,"../extensions")),builder.setCacheFile(join(__dirname,".build-cache.json")),builder.executeTask(["tsc","lessc","file","compress"]);