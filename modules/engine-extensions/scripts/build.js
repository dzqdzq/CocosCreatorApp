"use strict";const{join:join}=require("path"),{readFileSync:readFileSync,existsSync:existsSync,writeFileSync:writeFileSync,readdirSync:readdirSync,statSync:statSync}=require("fs"),{execSync:execSync}=require("child_process"),configFile=join(__dirname,"../config.json");existsSync(configFile)||(console.error("没有找到 config.json，无法正常构建"),process.exit(-1));try{const e=readFileSync(configFile),i=JSON.parse(e);let n="";n+=`/// <reference path="${join(i.enginePath,"./bin/.declarations/cc.d.ts")}"/>\n`,n+=`/// <reference path="${join(i.enginePath,"/bin/.declarations/cc.editor.d.ts")}"/>\n`,writeFileSync(join(__dirname,"../@types/cc.d.ts"),n)}catch(e){console.error("config.json 格式错误"),console.error(e),process.exit(-1)}process.argv.some(e=>e.startsWith("--only-dts"))&&process.exit(0);const builder=require("@editor/build");function scan(e){readdirSync(e).forEach(i=>{const n=join(e,i);statSync(n).isDirectory()&&builder.addWorkspace(n)})}scan(join(__dirname,"../extensions")),builder.setCacheFile(join(__dirname,".build-cache.json")),builder.executeTask(["tsc","lessc","file","compress"]);