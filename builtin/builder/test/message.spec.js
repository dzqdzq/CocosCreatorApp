"use strict";const{expect:expect}=require("chai"),{existsSync:existsSync}=require("fs-extra"),{join:join}=require("path"),{renameSync:renameSync}=require("fs"),{removeSync:removeSync}=require("fs-extra");describe("构建消息测试",()=>{describe("migrate-options",async()=>{const e=await Editor.Message.request("builder","migrate-options",{buildPath:"build",__version__:"1.2.6"});it("buildPath=build 数据迁移",()=>{expect(e.buildPath="project://build")})}),describe("generate-preview-setting",async()=>{const e=await Editor.Message.request("builder","generate-preview-setting",{debug:!0,startScene:"current_scene"});it("正确生成 settings",()=>{expect(e.settings).to.be.not.undefined}),it("正确生成 script2library",()=>{expect(e.script2library).to.be.not.undefined})})});